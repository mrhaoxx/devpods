---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: users.devpod.io
spec:
  group: devpod.io
  names:
    kind: User
    listKind: UserList
    plural: users
    shortNames:
    - du
    singular: user
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.devPodCount
      name: DevPods
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: User is the schema for DevPod users.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserSpec defines the desired state of a DevPod user.
            properties:
              displayName:
                description: DisplayName is a cosmetic label for UIs.
                type: string
              oidcSubject:
                description: |-
                  OIDCSubject is reserved for a future OIDC binding. The v1alpha1
                  controller does nothing with this value.
                type: string
              pubkeys:
                description: |-
                  Pubkeys is the list of OpenSSH-format authorized public keys for
                  this user. May be empty: the webui auto-provisions keyless Users
                  on first OAuth login, and the gateway falls back to LDAP (or
                  denies) when no key matches.
                items:
                  type: string
                type: array
              quota:
                description: |-
                  Quota caps this user's aggregate DevPod resources. nil = webui
                  global defaults.
                properties:
                  compute:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Compute caps the SUM of container resource limits (containers +
                      initContainers) across the user's *running* DevPods. Keys: cpu,
                      memory, and extended resources such as nvidia.com/gpu.
                    type: object
                  maxDevPods:
                    description: MaxDevPods limits how many DevPod CRs the user may
                      own.
                    format: int32
                    type: integer
                  storage:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      Storage caps the SUM of spec.persistence.size across ALL of the
                      user's DevPods, hibernated included (PVCs survive hibernation).
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
            type: object
          status:
            description: UserStatus reports observed state.
            properties:
              conditions:
                description: Conditions is the standard list of conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              devPodCount:
                description: DevPodCount is the number of DevPods currently owned
                  by this user.
                format: int32
                type: integer
            type: object
        type: object
        x-kubernetes-validations:
        - message: user name must match [a-z0-9-]{1,32}
          rule: self.metadata.name.matches('^[a-z0-9-]{1,32}$')
    served: true
    storage: true
    subresources:
      status: {}
